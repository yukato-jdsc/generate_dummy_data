## このプロジェクトについて
ETL で利用する初期データと差分データのCSVファイルを生成します。

## ドキュメント類
- 出力するCSVのフォーマット: @docs/format.md
- ETLの仕様書: @etl/docs
- DBのDDL、ストアドプロシージャ: @etl/ddl

## 必須ルール
- 必ず `pydoc` を書くこと（日本語）
- 単体テストでは長時間(30秒以上)かかるようなテストはしないこと
- 実装後について
  - `uv run ruff check .` を行ってチェックを行うこと

## その他
see: @README.md
